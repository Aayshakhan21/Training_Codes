<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         version="6.0">

    <!-- App-wide (ServletContext) -->
    <context-param>
        <param-name>company_name</param-name>
        <param-value>QuickCart</param-value>
    </context-param>

    <!-- Login -->
    <servlet>
        <servlet-name>LoginDashboard</servlet-name>
        <servlet-class>com.ecomm.servlet.LoginDashboard</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>LoginDashboard</servlet-name>
        <url-pattern>/logindashboard</url-pattern>
    </servlet-mapping>

    <!-- User Dashboard -->
    <servlet>
        <servlet-name>UserDashboard</servlet-name>
        <servlet-class>com.ecomm.servlet.UserDashboard</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>UserDashboard</servlet-name>
        <url-pattern>/user</url-pattern>
    </servlet-mapping>

    <!-- Product Dashboard -->
    <servlet>
        <servlet-name>ProductsDashboard</servlet-name>
        <servlet-class>com.ecomm.servlet.ProductsDashboard</servlet-class>

        <!-- Per-servlet (ServletConfig) -->
        <init-param>
            <param-name>ProductCount</param-name>
            <param-value>5</param-value>
        </init-param>
    </servlet>
    <servlet-mapping>
        <servlet-name>ProductsDashboard</servlet-name>
        <url-pattern>/products</url-pattern>
    </servlet-mapping>

    <!-- Default page -->
    <welcome-file-list>
        <welcome-file>login.jsp</welcome-file>
    </welcome-file-list>

</web-app>
# SpringLifeCycleScope

## Concept and Topic Notes
This project demonstrates bean lifecycle and scope concepts in Spring.
- Bean initialization and destroy callbacks
- Singleton vs Prototype scope behavior
- Scope impact in container-managed objects

## Key Annotations / Tags Used
- `@Autowired`
- `@Component`
- `@ComponentScan`
- `@Configuration`
- `@PostConstruct`
- `@PreDestroy`
- `@Scope`
- `@Service`

## Package / Module Overview
- `src/main/java` : Java source and module logic
- `src/main/resources` : properties/xml configuration

## How to Use
1. Import as Maven project.
2. Build with `mvn clean install`.
3. Run main/test classes from IDE.

## Objective
- Understand project-specific concepts, topics, and annotation usage through hands-on practice.
